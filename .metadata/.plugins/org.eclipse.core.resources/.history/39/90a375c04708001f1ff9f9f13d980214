package com.kh.BasicEx;
/*
 Import : 다른 패키지(폴더) 에 작성된 클래스가 필요할 때 쓰이는 구문이다.
 
 	사용 예제
 	 import 패키지명.클래스명;
 	 
 	 import java.Util.ArrayList; 
 	 // java 폴더 하위 Util 폴더 안에 있는 ArrayList 를 사용한다는 의미이다.
 	 // 똑같은 폴더 경로 위치라고 하더라도, 가져올 파일을 선택해서 가져오길 원한다면 이와 같이 하나하나 지정해주면 된다.
 	 
 	 import java.Util.*;
 	 // java 폴더 하위 Util 폴더 안에 있는 모든 파일을 가져올 수 있다는 의미이다.
 	 // * 를 썼을 때 바로 모든 파일을 가지고 오게 되면, 프로그램 성능이 저하될 수 있기 때문에,
 	 // 모두 가져오는 것이 아니라 모두 가져올 수 있도록 준비만 완료된다.
 	 // 파일 위치 경로 설정으로 이해하면 된다.
 */
public class Import {

}
