package com.kh.FileEx;
/*
 File(파일) : 폴더나 이미지, 텍스트, 동영상 등 만들고, 읽고, 쓰는 행위에 사용된다.
 
 	메서드
 		.exists() : 파일이나 폴더가 존재하는지 확인 여부를 반환할 때 사용한다.
 		 .mkdir() : 폴더 생성하기
 		.mkdirs() : 폴더를 한 번에 여러개 생성하기
 		
 FileWriter(파일을 만드는 객체) : 파일에 문자 데이터를 작성하고 싶을 때 사용한다.
                            파일에 텍스트를 작성하고, 저장할 때 사용한다.
                            
 	메서드
 		.write() : 파일 내용을 작성할 때 사용한다.
 		.flush() : 나머지 출력 스트림 버퍼를 비워내, 모든 데이터를 파일에 사용한다.
 		.close() : 출력 스트림을 닫고 저장할 때 사용한다.
 		
 	FileWriter 새로운변수명 = new FileWrite(파일변수명); // 파일을 덮어쓰기 한 다음 새로 만든다.
 	FileWriter 새로운변수명 = new FileWrite(파일변수명, true); // 파일에 이미 내용이 존재하면, 그 뒤에 이어서 작성한다.
 */
public class 파일 {
	
}
